/// <reference types="Cypress" />

context('Event 1000', () => {
  beforeEach(() => {
    cy.visit('/page/1000/event/1')
  })

  it('loads with correct page 1 content', () => {
  })

  it('starts with correct page 1 defaults', () => {
  })

  it('adds a maximum of 1 ticket', () => {
  })

  it('cannot have negative tickets', () => {
  })

  it('validates required page 1 inputs', () => {
  })

  it('works with max page 1 inputs', () => {
  })

  it('validates max+1 page 1 inputs', () => {
  })

  it('loads with correct page 2 content', () => {
  })

  it('starts with correct page 2 defaults', () => {
  })

  it('validates required page 2 inputs', () => {
  })

  it('works with max page 2 inputs', () => {
  })

  it('validates max+1 page 2 inputs', () => {
  })

  it('calculates correctly', () => {
  })

  it('calculates with donation correctly', () => {
  })

  it('calculates with code correctly', () => {
  })

  it('calculates with donation and code correctly', () => {
  })

  it('completes to the thank you page', () => {
  })

  it('gracefully handles double clicks', () => {
  })

  it('works with all payment options', () => {
  })

  it('handles back, forward and reload buttons', () => {
  })

  it('looks okay on mobile', () => {
  })

  it('can replace an alert dialog with a console log', () => {
  })
})
